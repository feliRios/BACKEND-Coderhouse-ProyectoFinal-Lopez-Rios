<div class="mt-24 relative">
    <div class="absolute inset-x-0 top-1/3 h-48 bg-gradient-to-r from-purple-300 to-purple-500 rounded-full blur-3xl opacity-30"></div>
    <div class="absolute inset-x-0 bottom-0 h-48 bg-gradient-to-r from-green-300 to-green-500 rounded-full blur-3xl opacity-30"></div>

    <div class="flex flex-col lg:flex-row space-x-0 m-8 lg:space-x-4 space-y-4 lg:space-y-0 justify-center">
        <div class="flex flex-col bg-white p-6 overflow-y-auto rounded-2xl shadow-lg justify-center items-center">
            <h1 class="text-gray-800 text-3xl font-bold mb-4">Gracias por confiar en nosotros!</h1>
            <div class="flex flex-col gap-3">
                <div class="flex flex-col w-full">
                    <div class="w-full flex items-center">
                        <div class="h-24 w-24 rounded-full bg-green-100 flex items-center justify-center">
                            <div class="h-14 w-14 rounded-full bg-green-500 flex items-center justify-center">
                                <span class="text-4xl font-bold text-white">
                                    <i class="fas fa-check"></i>
                                </span>
                            </div>
                        </div>
                        <div class="flex flex-col gap-1 ml-3">
                            <p class="px-4 py-1 rounded-xl text-gray-600 text-xs bg-gray-100 font-medium">
                                Tu ID de compra es <span class="text-orange-400 font-bold">{{ticket.id}}</span>
                            </p>
                            <p class="px-4 py-1 rounded-xl text-gray-600 text-xs bg-gray-100 font-medium">
                                Compraste un total de: <span class="text-green-400 font-bold">{{ticket.totalItems}}</span> productos
                            </p>
                            <p class="px-4 py-1 rounded-xl text-gray-600 text-xs bg-gray-100 font-medium">
                                Gastaste un total de: <span class="text-green-400 font-bold">{{ticket.amount}}</span> USD
                            </p>
                            <p class="px-4 py-1 rounded-xl text-gray-600 text-xs bg-gray-100 font-medium">
                                Comprador: <span class="text-green-400 font-bold">{{ticket.first_name}} {{ticket.last_name}}</span>
                            </p>
                            <p class="px-4 py-1 rounded-xl text-gray-600 text-xs bg-gray-100 font-medium">
                                Email: <span class="text-green-400 font-bold">{{ticket.purchaser}}</span>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
